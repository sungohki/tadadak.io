export interface KadviceStorageServiceModel {
    /**
     * @params max - 최대 인덱스
     * @returns index - 현재 표시될 인덱스
     */
    getCurrentAdviceIndex: (max: number) => number;
}
export declare class KadviceStorageService implements KadviceStorageServiceModel {
    private countKey;
    private midNightKey;
    constructor(countKey?: string, midNightKey?: string);
    getCurrentAdviceIndex(max: number): number;
    private initializeCountAndMidnightKeys;
    private getCurrentMidNightTime;
    private getStorage;
    private setStorage;
}
export declare function getKadviceStorageService(cntKey?: string, midNightKey?: string): KadviceStorageService;
